#include <stdlib.h>
#include <stdio.h>

int main(int argc, char *argv[]) {
  // int dolartoclp = 663;
  // int pesos;
  // int pesos = atoi(argv[1]) * atoi(argv[1]);
  printf("%i\n", atoi(argv[1]) * atoi(argv[2]));

  return 0;
}
